{
  "name": "daily_meme",
  "version": "1.0.0",
  "description": "Funny ai agent for every day key events meme generation",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "node": "cd packages/hardhat && npx hardhat node",
    "deploy": "cd packages/hardhat && npx hardhat ignition deploy ignition/modules/AidleTokenModule.ts --network localhost"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
