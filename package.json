{
  "name": "aidle",
  "version": "1.0.0",
  "description": "Funny ai agent for every day key events meme generation",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "node": "npm run node --workspace=packages/hardhat",
    "deploy": "npm run deploy --workspace=packages/hardhat",
    "dev": "npm run dev --workspace=packages/frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
